{
  "name": "pos-client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "vercel-build": "npm run build",
    "preview": "vite preview"
  },
  "dependencies": {
    "qrcode.react": "^3.1.0"
  },
  "devDependencies": {
    "@types/qrcode.react": "^1.0.2"
  }
}